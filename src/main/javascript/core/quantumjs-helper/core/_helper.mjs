/**
 * Imports a class only once.
 *
 * @param name The name of the class
 */
function import_once(name: string): any {
    // Stub
}

/**
 * Transforms an object into another object.
 *
 * @param value
 */
function transform(value: object): any {
    // Stub
}

//
// Aesthetics for Java + Javascript interop.
//

/**
 * Marks a function as overriding another function in the superclass.
 *
 * @param func The function to decorate
 */
function override(func: object): any {
    // Stub
}

/**
 * Marks a class as final.
 *
 * @param clazz The class to decorate
 */
function final(clazz: object): any {
    // Stub
}